module merkly::onft_test {
    // #[test_only]
    // use merkly::onft;
    // #[test_only]
    // use aptos_framework::coin::{MintCapability, BurnCapability};
    // #[test_only]
    // use aptos_framework::aptos_coin::AptosCoin;
    // #[test_only]
    // use aptos_framework::account::{Self, SignerCapability};
    // #[test_only]
    // use merkly::onft::{State, MERKLY_SEED};
    // #[test_only]
    // use aptos_std::simple_map::{Self, SimpleMap};
    // #[test_only]
    // use aptos_framework::event::{Self, EventHandle};
    // #[test_only]
    // struct AptosCoinCap has key {
    //     mint_cap: MintCapability<AptosCoin>,
    //     burn_cap: BurnCapability<AptosCoin>,
    // }

    

    // #[test_only]
    // fun test_mint(aptos: &signer, core_resources: &signer, addresses: vector<address>) {
        
    // }

    // #[test_only]
    // fun test_send(aptos: &signer, core_resources: &signer, addresses: vector<address>) {
        
    // }
}